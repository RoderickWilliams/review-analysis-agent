# ===========================================
# 全平台用户反馈智能分析 Agent - Python 依赖
# ===========================================
# Python 版本要求: 3.10+
# 安装方式: pip install -r requirements.txt
# 安装浏览器: patchright install chromium
# ===========================================

# --- 核心 LLM ---
openai>=1.0.0

# --- 数据处理 ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
jieba>=0.42.1
pyarrow>=7.0,<25

# --- Web 请求与爬虫 ---
requests>=2.31.0
beautifulsoup4>=4.12.0
curl_cffi>=0.6.0
selenium>=4.15.0
webdriver-manager>=4.0.0
DrissionPage>=4.0.0
fake-useragent>=1.0.0

# --- 浏览器自动化（反检测） ---
# Patchright 是 Playwright 的反检测 fork，drop-in 替换
# 安装后执行: patchright install chromium
patchright>=1.50.0
# Playwright 作为回退（patchright 不可用时自动降级）
playwright>=1.40.0

# --- OCR 文字识别 ---
pytesseract>=0.3.10
Pillow>=10.0.0

# --- Web UI (Streamlit) ---
streamlit>=1.30.0
altair>=5.0.0
pydeck>=0.8.0

# --- Web API (FastAPI) ---
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# --- 文档导出 ---
python-docx>=1.0.0
pymupdf>=1.24.0
openpyxl>=3.1.0

# --- 工具库 ---
python-dotenv>=1.0.0
tenacity>=8.1.0
watchdog>=2.1.5
tqdm>=4.66.0
mss>=9.0.0
psutil>=5.9.0
wasmtime>=20.0.0

# --- 可选: PaddleOCR（需要 Python 3.10~3.13，不支持 3.14）---
# 如需使用 PaddleOCR 作为首选 OCR 引擎，请安装:
#   pip install paddlepaddle==3.2.0 paddleocr -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# paddlepaddle>=3.0.0
# paddleocr>=3.0.0

# --- 可选: Flask (web_proxy_server.py 使用，有 try-except 降级) ---
# flask>=3.0.0
